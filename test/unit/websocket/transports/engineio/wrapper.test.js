describe("wrapper", function(){

  describe("#connect", function(){

    describe("open event", function(){

      it("should retrieve the session id from the connect.sid cookie");
      // We need a way to simulate the browser
      // We need to set a cookie on the browser
      // We need to load the javascript into that browser's page
      // We need to run the script which attempts to connect to the server

      it("should send a message back to the server with the session id, if a session id is present");

      it("should raise an error if if is not able to obtain a connect session id");

    });

  });

});
