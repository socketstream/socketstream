describe('packed client',function() {

  it('should be served when option servePacked or packAssets set');

  it('should serve JS ondemand within bounds defined');
  
  it('should serve worker JS in definitions');

  it('should include libs in client');
});
