module.exports = function(ss) {
	var strategy = {
		sessionMiddleware: [
			sessionMiddleware
		],

		create: function() {
			return {};
		}
	};
	ss.session.setStrategy(strategy);

	function sessionMiddleware(req,res,next) {
		return next();
	}

};