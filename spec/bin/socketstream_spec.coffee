require '../../bin/socketstream'
describe 'AppGenerator', ->
  
  it 'should generate a new application in the given directory', ->
    # so, go to a directory that you can pollute and destroy
    # then, create the following folders and files:
      # app/
      #   client/
      #     app.coffee
      #   sass/
      #     app.sass
      #   server/
      #     app.coffee
      #   views/
      #     index.jade
      #   app.coffee
      # lib/
      #   client/
      #   css/
      #     reset.css
      #   server/
      #   nodemon-ignore
      # public/
      #  assets/
      #  dev/
      # vendor/
      
  