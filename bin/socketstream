#!/usr/bin/env node
var util          = require('util');
var fs            = require('fs');

var argParser     = require('argsparser@0.0.3');

var lib           = __dirname + '/../lib';
var args          = argParser.parse().node;

require('coffee-script@1.0.0');
require(lib + '/cli.coffee').process(args);